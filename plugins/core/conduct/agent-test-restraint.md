# Agent Test Restraint

Do not create or run tests unless the user explicitly asks.

This prevents AI agents from proactively generating test files or executing test suites when not requested, avoiding unwanted changes and noise in the codebase.
